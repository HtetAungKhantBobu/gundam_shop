<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand brand-name" href="/">Side7</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link <%- (page_title=== 'Home')?'active':'' %>" aria-current="page" href="/">Home
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%- (page_title==='Products' )?'active':'' %>" href="/products">Products</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        Contact Us
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="#">Feedback</a></li>
                        <li><a class="dropdown-item" href="#">Careers</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="#">Contact Sales</a></li>
                    </ul>
                </li>
            </ul>
            <!-- <form class="d-flex">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form> -->
            <% if (page_title==="Products" ) { %>
            <form class="d-flex" id="search_bar">
                <div class="input-group">
                    <input class="form-control" name="keyword" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-sm btn-outline-primary" type="submit">Search</button>
                </div>


            </form>

            <% } %>
            <% if (!locals.user) { %>
            <a id="login-btn" class="btn btn-outline-success" href="/login" role="button">Sign In</a>

            <% } else { %>

            <% if (user.role==="admin" ) { %>

            <%- include('add-product-modal') %>
            <% } %>
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="/logout" id="navbarProfileDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        Hi, <%= user.username %>
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li>
                            <a name="" id="" class="dropdown-item" href="/logout" role="button">Logout</a>
                        </li>
                    </ul>
                </li>
            </ul>
            <% } %>

        </div>
    </div>
</nav>
<!-- 
db.users.updateMany({}, {$set: {"role": "user"}});
db.users.updateOne({"username": "Admin"}, {$set: {"role": "admin"}});
-->